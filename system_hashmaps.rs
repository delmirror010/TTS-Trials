use std::collections::HashMap;

let mut did_sys = HashMap::new();
// check message author what variable type
did_sys.insert(String::from("id?"), a_sys);

let mut a_sys = HashMap::new();
a_sys.insert(String::from("-y"), "Yuuen");

pub fn get_member_nickname(shorthand: &str, msg_author: &str) {

  
  
}

